Basic_form = Ext.extend(Basic_formUI,{
  constructor: function  (config) {
    config = config || {};
    Ext.apply(this, config);
    Basic_form.superclass.constructor.call(this)
  },
  
  initComponent: function(){
    Basic_form.superclass.initComponent.call(this);
    var self = x = this;
    
  }
});
