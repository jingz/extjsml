 
Basic_window = Ext.extend(Basic_windowUI,{
  constructor: function  (config) {
    config = config || {};
    Ext.apply(this, config);
    Basic_window.superclass.constructor.call(this)
  },
  
  initComponent: function(){
    Basic_window.superclass.initComponent.call(this);
    var self = x = this;
    // use x for debugging at console
    
    
  }
});
